import { withObservedProperties } from './index.js';

describe('withObservedProperties', () => {
  it('Should not be undefined.', () => {
    expect(withObservedProperties).to.not.be.undefined;
  });
});
